apiVersion: v1
kind: Service
metadata:
  name: vote-ui
  namespace: voting-app
  labels:
    app: vote-ui
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31000
  selector:
    app: vote-ui
