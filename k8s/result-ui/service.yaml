apiVersion: v1
kind: Service
metadata:
  name: result-ui
  namespace: voting-app
  labels:
    app: result-ui
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31001
  selector:
    app: result-ui
